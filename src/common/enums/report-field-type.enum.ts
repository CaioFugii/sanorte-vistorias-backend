export enum ReportFieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  DATE = 'date',
  DATETIME = 'datetime',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  IMAGE = 'image',
  SIGNATURE = 'signature',
}
